import * as utils2 from '../shared/utils2.js';

import { canvas, ctx } from './engine.js';

let img = null;

export async function init()
{
	//const div = document.getElementById('chatUnder');

	//img = await utils2.loadImage("./assets/UI/ChatBar/inputArea.png");


}

export function update()
{
	/*ctx.drawImage(
		img,
		//元画像
		0, 0, img.width, img.height,
		//キャンバス位置
		200, 200, img.width, img.height
	);*/
}

//showModelDebugInfo();
//setInterval(showModelDebugInfo, 500);